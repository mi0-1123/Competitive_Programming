#include<iostream>
#include<stdio.h>
#include<vector>
#include<string.h>
#include<algorithm>
using namespace std;

int main(){
  /* int n;
   * cin >> n;
   *vector<char> a(1024);
   *for(int i=0;i<nli++){
   *cin >> a;
   *for(int j=0;i<a.length();j++){
   */  


  int n,i,len;
  char in[2000][20000];

  scanf("%d",&n);
  for(i=0;i<n;i++) scanf("%s",in[i]);
  for(i=0;i<n;i++){
    len=strlen(in[i]);
    for()
